import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function ActPage() {
  return (
    <ModulePlaceholder
      title="Act"
      phase="Phase 3"
      blurb="An agent with geo tools (geocode, route, isochrone, nearby, weather) exposed over MCP and used in-app."
    />
  );
}
