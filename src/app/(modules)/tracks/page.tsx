import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function TracksPage() {
  return (
    <ModulePlaceholder
      title="Tracks"
      phase="Phase 4"
      blurb="Import GPS trajectories, compute movement metrics, play them back, and get an AI briefing."
    />
  );
}
